PER_PAGE=9
SECRET_KEY='CHANGE-ME'
# Generate SECRET_KEY VARIABLE (use this command in terminal inside environment and together)
# python -c 
# "import string as s;from random import SystemRandom as sr;print(''.join(sr().choices(s.ascii_letters + s.punctuation, k=64)))"


SECRET_KEY_JWT='CHANGE-ME'

# 0 = False
# 1 = True
DEBUG = 1
SELENIUM_HEADLESS=0


# Comma separated values


ALLOWED_HOSTS = 'localhost, 127.0.0.1'
CSRF_TRUSTED_ORIGINS = 'https://localhost,'
CORS_ALLOWED_ORIGINS = 'http://localhost:8000, '